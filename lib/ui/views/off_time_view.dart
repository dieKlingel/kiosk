import 'package:flutter/material.dart';

class OffTimeView extends StatelessWidget {
  const OffTimeView({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(KoiFresh): implement build
    throw UnimplementedError();
  }
}
